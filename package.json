{
  "name": "cityops-ai",
  "version": "0.1.0",
  "private": true,
  "description": "CityOps AI — AI-powered city operations management platform",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "setup": "chmod +x scripts/setup.sh && ./scripts/setup.sh",
    "dev": "chmod +x scripts/dev.sh && ./scripts/dev.sh",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "format": "npm run format -w backend",
    "test": "npm run test -w backend",
    "clean": "npm run clean -w backend && npm run clean -w shared && npm run clean -w frontend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
