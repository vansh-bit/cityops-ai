rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Operational collections are backend-owned only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
