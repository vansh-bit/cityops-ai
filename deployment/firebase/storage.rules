rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Client access to operational assets is prohibited.
    // Uploads, reads, and signed URL generation are backend-owned.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
