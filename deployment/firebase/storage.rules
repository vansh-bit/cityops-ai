rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default: deny all reads and writes
    // Rules will be configured in future milestones
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
